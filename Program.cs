using static System.Console; //Assim não precisa escrever Console. antes de imprimir o que foi lido acima
public class Program{
    static void Demo1(){
  int a = 2;
       a = Adicionar20(a);

        WriteLine($"O valor da variável a é {a}");
    }
static int Adicionar20(int x)
{
   return x + 20;
} 
    public static void main()
    {
      
    }
}
    
